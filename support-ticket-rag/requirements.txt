pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
tabulate>=0.9

# Production stack (offline fallbacks are used automatically if these
# aren't installed — see README for what each one upgrades)
transformers>=4.40
torch>=2.2
langchain>=0.2
langchain-openai>=0.1
openai>=1.30
chromadb>=0.5
gradio>=4.30
